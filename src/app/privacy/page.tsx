import { FileWarning, KeyRound, LockKeyhole, ShieldCheck, Trash2 } from "lucide-react";

export default function PrivacyPage(){return <div className="page"><div className="narrow"><div className="page-header"><div><p className="eyebrow">Privacy & security</p><h1>Study material deserves careful handling.</h1><p className="lede">StudyForge is designed so an academic document is a private source—not a command and never a public artifact.</p></div></div><div className="review-list">{[[LockKeyhole,"Private by default","Guest guides and attempts stay in local browser storage. Signed-in cloud rows are protected by per-user row-level security."],[FileWarning,"Documents are untrusted input","Instructions embedded in uploads are neutralized and treated as study content, not application commands."],[KeyRound,"Secrets stay server-side","The OpenAI key is read only by the server generation endpoint and never shipped in client JavaScript."],[ShieldCheck,"Validated at every boundary","File size and type, form input, generated questions, and database ownership are checked before use."],[Trash2,"You control deletion","Study guides require confirmation before permanent deletion. Supabase policies and the included account-deletion function support stored-data removal."]].map(([Icon,title,body])=><article className="card" key={String(title)} style={{display:"grid",gridTemplateColumns:"auto 1fr",gap:16}}>{typeof Icon!=="string"&&<span className="feature-icon" style={{margin:0}}><Icon size={20}/></span>}<div><h3>{String(title)}</h3><p className="muted" style={{marginBottom:0}}>{String(body)}</p></div></article>)}</div><div className="card" style={{marginTop:18}}><h2 style={{fontSize:"1.5rem"}}>Operational note</h2><p className="muted">When the optional OpenAI provider is configured, selected source text is sent from the secure server endpoint to generate the requested quiz. Without a key, the local grounded generator runs instead. Do not upload material you are not permitted to process.</p></div></div></div>}
