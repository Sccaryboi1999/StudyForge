import { Check, Cloud, History, SlidersHorizontal, Target } from "lucide-react";
import { AuthForm } from "@/components/auth-form";

export default function AuthPage(){return <div className="page"><div className="container" style={{maxWidth:980}}><div className="two-col" style={{alignItems:"center"}}><div><p className="eyebrow">Optional StudyForge account</p><h1 style={{fontSize:"clamp(2.2rem,5vw,4rem)"}}>Keep your progress beyond this browser.</h1><p className="lede">Guest mode is complete. Sign in only when you want long-term history and cross-device study.</p><div style={{marginTop:30}}>{[[Cloud,"Cross-device access"],[History,"Saved quiz history"],[Target,"Weak-topic analysis"],[SlidersHorizontal,"Reusable quiz presets"]].map(([Icon,label])=><div className="toggle-row" key={String(label)}><span style={{display:"flex",alignItems:"center",gap:10}}>{typeof Icon!=="string"&&<Icon size={18} color="var(--accent)"/>}{String(label)}</span><Check size={17} color="var(--mint)"/></div>)}</div></div><AuthForm/></div></div></div>}
